import React, { Component } from 'react';

class FavRow extends Component {

    like() {

      // *Update table*
      // get asc/desc component prop - what is the order
      // iterate Fav items by this order
      // insert Fav

      
      
      }

    render() {
        return <button onClick={this.like}>like</button>;
      }

}